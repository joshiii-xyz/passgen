[package]
name = "passgen"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Generate secure passwords from the terminal"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
rand = "0.8"
